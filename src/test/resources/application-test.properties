# Use same DB but separate schema for tests
spring.datasource.url=jdbc:postgresql://localhost:5432/feedback_flow_db
spring.datasource.username=feedback_user
spring.datasource.password=feedback123
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

# JWT
app.jwt.secret=test-secret-key-must-be-at-least-32-characters-long-for-tests
app.jwt.expiration-ms=86400000

# Disable real emails in tests
spring.mail.host=localhost
spring.mail.port=3025
spring.mail.username=test
spring.mail.password=test